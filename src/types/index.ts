export interface Theme {
    dark: string,
    darkAccent: string,
    primary: string,
    light: string,
    faded: string,
    lightFade: string,
    confirmation: string,
    danger: string,
    background: {
        backgroundColor: string,
        flex: number,
        padding: number,
    }
}